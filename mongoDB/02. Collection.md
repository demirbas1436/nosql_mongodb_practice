# Collection

### 📁 Koleksiyon (Collection) Nedir?
- Klasör gibi düşünebilirsin.

- Aynı türden belgeleri (document) bir arada tutar.

- SQL'deki “tablo”ya benzer ama daha esnektir.

- Örneğin: `kullanicilar, urunler, siparisler` gibi koleksiyonlar olabilir.


### 📄 Belge (Document) Nedir?
- Koleksiyon içindeki tek bir veri kaydıdır.

- JSON benzeri bir yapıda saklanır (aslında BSON formatında).

- SQL'deki “satır” (row) gibi düşünülebilir ama daha esnektir.

- Her belge kendi alanlarına (field) sahip olabilir ve bu alanlar diğer belgelerden farklı olabilir.

Diyelim ki bir kullanicilar koleksiyonumuz var. İçinde şu belgeler olabilir:
```json
// 1. belge
{
  "ad": "Ali",
  "yas": 30,
  "email": "ali@example.com"
}

// 2. belge
{
  "ad": "Ayşe",
  "yas": 25,
  "telefon": "555-1234"
}
```
Bu iki belge aynı koleksiyonda yer alır ama alanları farklı olabilir.

>#### MongoDB bu esnekliğe izin verir; her belge aynı yapıda olmak zorunda değildir.


| SQL Terimi	 | MongoDB Terimi   |
|----------------|------------------|
| Veritabanı	 | Veritabanı       |
| Tablo		     | Koleksiyon       | 
| Satır (Row)	 | Belge (Document) |
| Sütun (Column) | Alan (Field)     |


MongoDB verileri şu şekilde hiyerarşik bir yapıda tutar:
```
                        MongoDB Sunucusu
                        └── Veritabanı (Database)
                            └── Koleksiyon (Collection)
                                    └── Belge (Document)
```
MongoDB = Bir kutular sistemi gibi:

`Kutular (Veritabanı)` → `içinde bölümler (Koleksiyon)` → `onların içinde veriler (Belgeler)` vardır.
