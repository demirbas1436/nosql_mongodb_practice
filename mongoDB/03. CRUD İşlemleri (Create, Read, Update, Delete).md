# CRUD İşlemleri (Create, Read, Update, Delete)

- CRUD, veriyle ilgili temel işlemleri ifade eder:
    - C → Create: Yeni veri ekleme
    - R → Read: Veri okuma / sorgulama
    - U → Update: Var olan veriyi güncelleme
    - D → Delete: Veri silme

MongoDB’de bu işlemler koleksiyon (collection) üzerinden yapılır.

-------------------------------------------------------------------------
## 🟢 CREATE (Veri Ekleme)

MongoDB’de veri eklemek için aşağıdaki metotlar kullanılır:

- **`insertOne()`** → Tek belge ekler  
- **`insertMany()`** → Birden fazla belge ekler  


### 📌 Veritabanını Seçme

```js
use universiteDB
```


### ➕ Tek Belge Ekleme

```js
db.ogrenciler.insertOne({
  ad: "Ahmet",
  soyad: "Yılmaz",
  yas: 21,
  bolum: "Bilgisayar Mühendisliği"
})
```


### ➕➕ Birden Fazla Belge Ekleme

```js
db.ogrenciler.insertMany([
  {
    ad: "Ayşe",
    yas: 22,
    bolum: "Makine Mühendisliği"
  },
  {
    ad: "Mehmet",
    yas: 20,
    bolum: "Elektrik-Elektronik"
  }
])
```
## 📘 Notlar

- MongoDB belgeleri **JSON benzeri (BSON)** yapıda saklanır.
- `_id` alanı otomatik olarak oluşturulur.
- Alanlar şemasızdır, her belge farklı alanlara sahip olabilir.
-------------------------------------------------------------------------

## 🔵 READ (Veri Okuma)

MongoDB’de veri okumak için kullanılan metotlar:

- **`find()`** → Belgeleri listeler  
- **`findOne()`** → Tek bir belge getirir  
- **`pretty()`** → Sonuçları daha okunabilir yapar  

### 📄 Tüm Belgeleri Getir

```js
db.ogrenciler.find()
```


### 📄 Okunabilir (Pretty) Format

```js
db.ogrenciler.find().pretty()
```


### 🎯 Belirli Koşulla Sorgu

```js
db.ogrenciler.find({ bolum: "Bilgisayar Mühendisliği" })
```

Tek belge getir:

```js
db.ogrenciler.findOne({ ad: "Ahmet" })
```


### 🔢 Yaşı 21'den Büyük Olanlar

```js
db.ogrenciler.find({ yas: { $gt: 21 } })
```


### 🏫 Bölümü Bilgisayar Mühendisliği Olanlar

```js
db.ogrenciler.find({
  bolum: { $in: ["Bilgisayar Mühendisliği"] }
})
```

### 🧩 Sadece Belirli Alanları Göster (Projection)

```js
db.ogrenciler.find(
  {},
  { ad: 1, yas: 1, _id: 0 }
)
```

-------------------------------------------------------------------------

## 🟡 UPDATE (Veri Güncelleme)

MongoDB’de veri güncellemek için aşağıdaki metotlar kullanılır:

- **`updateOne()`** → Tek belgeyi günceller  
- **`updateMany()`** → Birden fazla belgeyi günceller  

Güncellemeler genellikle **`$set`**, **`$inc`**, **`$unset`** gibi operatörlerle yapılır.


## ✏️ Tek Belge Güncelleme

```js
db.ogrenciler.updateOne(
  { ad: "Ahmet" },      // Koşul
  { $set: { yas: 22 } } // Değişiklik
)
```

## ✏️✏️ Birden Fazla Belgeyi Güncelleme

```js
db.ogrenciler.updateMany(
  { bolum: "Bilgisayar Mühendisliği" },
  { $inc: { yas: 1 } } // Yaş değerini 1 artır
)
```

## 🗑️ Bir Alanı Silme

```js
db.ogrenciler.updateOne(
  { ad: "Ayşe" },
  { $unset: { bolum: "" } } // bolum alanını kaldır
)
```

## 📘 Notlar

- `$set` → Alan ekler veya günceller  
- `$inc` → Sayısal alanı artırır / azaltır  
- `$unset` → Alanı tamamen siler  
-------------------------------------------------------------------------
## 🔴 DELETE (Veri Silme)

MongoDB’de veri silmek için aşağıdaki metotlar kullanılır:

- **`deleteOne()`** → Tek belge siler  
- **`deleteMany()`** → Birden fazla belge siler  


## 🗑️ Tek Belge Silme

```js
db.ogrenciler.deleteOne({ ad: "Mehmet" })
```

## 🗑️🗑️ Birden Fazla Belge Silme

```js
db.ogrenciler.deleteMany(
  { yas: { $lt: 21 } } // 21 yaşından küçükleri sil
)
```
-------------------------------------------------------------------------
## 🔢 Sayma ve Sıralama

## 📊 Kaç Öğrenci Olduğunu Bulma

```js
db.ogrenciler.countDocuments()
```

## ⬆️ Yaşa Göre Artan Sırada Listeleme

```js
db.ogrenciler.find().sort({ yas: 1 })
```

## ⬇️ Yaşa Göre Azalan Sırada Listeleme

```js
db.ogrenciler.find().sort({ yas: -1 })
```

---
